version: '3'
services:
  mojolicious:
    build:
      context: ./apps/mojolicious/
      dockerfile: Dockerfile
    volumes:
      - ./apps/mojolicious/:/app
    ports:
      - ${MOJOLICIOUS_PORT}:${MOJOLICIOUS_PORT}